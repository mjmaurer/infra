session_name: ${WORKSPACE}-aider
windows:
  - window_name: aider
    options:
      automatic-rename: false
    panes:
      - shell_command:
          - cl --ide
  # - window_name: aider-watch
  #   panes:
  #     - shell_command:
  #         - aider --sonnet --watch-files 
