session_name: ${WORKSPACE}-ai
windows:
  - window_name: claude
    options:
      automatic-rename: false
    panes:
      - shell_command:
          - ai-sandbox cl --ide
  # - window_name: aider-watch
  #   panes:
  #     - shell_command:
  #         - aider --sonnet --watch-files 
