session_name: ai
windows:
  - window_name: ai
    layout: even-vertical
    options:
      automatic-rename: false
    panes:
      - shell_command:
          - cmd: 'llm chat -m gemini-2.5-flash-preview-05-20 "${AI_CMD}"'
        focus: true
      - shell_command:
          - cmd: 'llm chat -m gemini-2.5-pro-preview-05-06 "${AI_CMD}"'
      - shell_command:
          - cmd: 'llm chat -m o4-mini "${AI_CMD}"'
